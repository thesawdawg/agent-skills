{
  "description": "Injects D.A.V.E.'s current focus and priority list at session start, so the orchestrator is present from the first token rather than waiting to be summoned. Silent when D.A.V.E. is not set up, or when hooks.session_start is false in ~/.dave/config.json.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
