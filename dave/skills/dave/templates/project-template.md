# {{NAME}} — D.A.V.E. project instance

This directory tunes the globally installed D.A.V.E. for one project. The ranked
list, log, missions and time ledger stay global in `~/.dave`; this file and
`config.json` beside it change how D.A.V.E. behaves *here*. Read it at session
start, after `dave.sh brief`.

## Goal

<!-- One sentence: what finishing looks like. Mirrors the registered project's goal. -->
{{GOAL}}

## Scope — what counts as this project

<!-- Directories, repositories, services that are "inside". Work outside this list
     is a detour candidate for the drift watch. -->
- `{{PATH}}`

## Sources

<!-- Where this project's work items come from. Leave a line blank if not used. -->
- Redmine filter:
- Boards:

## Rules for D.A.V.E. in this project

<!-- Project-specific guardrails and habits. These add to, never override, the two
     things that are never automatic (outward writes; rewriting priorities.md). -->
- 

## Project roles

<!-- Roles in `roles/` beside this file, enabled via `config.json` `roster`.
     Each entry: what the role gates or maintains here, and when to reach for it. -->
- 
