# Folder Structure — <project name>

> Generated by Constructor. This is the concrete directory layout —
> actual directories, not abstract categories. The maintenance test:
> a new dev should find where to add a new feature in under 60 seconds.

## Tree

```
<project-root>/
├── <dir>/                  # <what goes here, in one line>
│   ├── <subdir>/           # <what goes here>
│   └── <file>              # <what this is>
├── <dir>/                  # <what goes here>
│   └── ...
├── tests/                  # <how tests are organized>
│   ├── <dir>/              # <mirrors source structure>
│   └── ...
├── config/                 # <configuration files>
├── <entry-point>           # <where the app starts>
└── README.md
```

## Where things go

| Thing | Location | Why |
|-------|----------|-----|
| Entry point | `<path>` | <one line> |
| Domain logic | `<path>` | <one line> |
| Data access | `<path>` | <one line> |
| HTTP handlers | `<path>` | <one line> |
| Configuration | `<path>` | <one line> |
| Shared utilities | `<path>` | <one line> |
| Tests | `<path>` | <one line> |

## The 60-second test

> A new dev needs to add `<example feature>`. Here's the path:
> 1. <where they look>
> 2. <what they create>
> 3. <what they wire up>
>
> If this takes more than 60 seconds, the structure needs simplifying.

## Conventions

- **Naming:** <convention for files, modules, etc.>
- **Module boundaries:** <how modules communicate — direct import /
  interface / event>
- **Test placement:** <mirror structure / central tests / co-located>
