# Documentation Index

Generated by the `commit-documentor` skill. Maps areas of **this** project to the
pages in the configured documentation repo that describe them, so a commit can be
matched to its docs locally without searching the whole doc repo every run.

- Doc repo: `<remote>` (branch `<branch>`, root `<docs_root>`)
- Doc repo commit at last scan: `<sha>` — `<subject>`
- Last scanned: `<YYYY-MM-DD>`

## Code area → documentation

| Code path (glob) | Doc file(s) | What the docs cover | Confidence |
|---|---|---|---|
| `src/api/**` | `docs/reference/api.md` | Endpoint list, request/response shapes | high |
| `src/config/**` | `docs/guides/configuration.md` | Env vars, config file schema | high |
| `src/cli/**` | `docs/reference/cli.md` | Command and flag reference | medium |

## Undocumented areas

Code areas with no matching doc page. A commit touching one of these is a
candidate for a *new* page rather than an edit.

- `src/workers/**` — no page found.

## Doc pages with no code owner

Pages in the doc repo that this project's index could not attribute to a code
area. Usually owned by another project — leave them alone unless told otherwise.

- `docs/legal/terms.md`
